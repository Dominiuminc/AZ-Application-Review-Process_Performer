Extract information from Screening Documents images to determine conditions and produce a JSON output following the provided schema.

- **Task Overview**: Analyze the provided document images, extract relevant data, and evaluate the specified conditions. Then format the results into the given JSON schema.

## Steps to Complete the Task

1. **Image Processing**:
   - Recognize and extract text from the document images using OCR (Optical Character Recognition).

2. **Logic for Determining Outputs**:
   - Extract "ApplicantInformation" from the relevant section under "PropertyScreeningResult"
   - Extract the full text of each bullet under "Items to review", without the bullet
   - Extract the searches under "Summary", just the first column named "Search Type" and the last column "Status"
   - Extract the Screening Works Pro results from the relevant page. The name will appear directly under "Employee Information" along with the SSN. "JobTitle" and "EmploymentStatus" can be found as fields in the following section.

## Output Format

The output must adhere to the following JSON structure (**replacing placeholder values with actual extracted data**):

```json
{
  "validation": {
    "form_present_for_all_adults": "boolean",
    "ssn_matches_member_info": "boolean", 
    "dob_matches_member_info": "boolean",
    "income_verification_search_type_not_null": "boolean",
    "status_complete": "boolean",
    "records_found_includes_additional_pages": "boolean",
    "employment_status_is_active": "boolean",
    "has_signature": "boolean",
    "has_date": "boolean"
  },
  "ApplicantInformation": {
    "Name": "string",
    "DOB": "MM/DD/YYYY",
    "SSN_last_4": "string"
  },
  "ItemsToReview": ["string"],
  "Searches": [
    {
        "SearchType": "string",
        "Status": "string"
    }
  ],
  "ScreeningWorksProResults": [
    {
        "EmployeeInformation": {
            "Name": "string",
            "SSN": "string"
        },
        "Employer": "string",
        "JobTitle": "string",
        "EmploymentStatus": "string"
    }
  ],
  "signature_section": {
    "signature_present": "boolean",
    "date_signed": "string"
  }
}
```

**Notes on Output**:
- Ensure boolean fields (`true` or `false`) are accurately set based on the extracted data and conditions.
- Include only this JSON output in the final response—no additional text or explanation.