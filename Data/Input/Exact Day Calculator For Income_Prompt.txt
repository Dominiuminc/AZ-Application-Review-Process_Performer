You are tasked with extracting all data from an "Exact Day Calculator for Income" form and converting it to structured JSON format. 

Please extract the following information and output it as valid JSON:

## Instructions:
1. Extract ALL visible data from the form, including empty fields (mark as null)
2. Preserve exact values, dates, and formatting
3. Include all table data, even if cells are empty
4. Maintain the hierarchical structure of the form
5. Use clear, descriptive field names
6. Format dates consistently as YYYY-MM-DD
7. Include monetary values as numbers (without $ symbols)
8. Mark empty/blank fields as null

## Expected JSON Structure:
```json
{
  "form_info": {
    "title": "string",
    "revision_date": "YYYY-MM-DD",
    "household_member": "string",
    "source_of_income": "string",
    "pay_schedule_option": "string"
  },
  "hourly_income_verification": {
    "purpose": "string",
    "pay_categories": [
      {
        "category": "string",
        "pay_rate": null,
        "hours_per_pay_period": null,
        "number_of_pay_periods": null,
        "total_per_year": null
      }
    ]
  },
  "check_stub_income_backup": {
    "purpose": "string",
    "pay_stubs": [
      {
        "start_date": "YYYY-MM-DD", 
        "end_date": "YYYY-MM-DD",
        "gross_pay": number
      }
    ],
    "totals": {
      "total_gross_pay": number,
      "oldest_pay_period_start": "YYYY-MM-DD",
      "most_recent_pay_period_end": "YYYY-MM-DD", 
      "total_pay_periods": number,
      "average_amount_per_pay_period": number,
      "yearly_using_pay_period_average": number,
      "highest_calculated_income_value": number
    }
  }
}
```

## Notes
-Extract all data from the provided form and return only the JSON output with no additional text or explanations.
